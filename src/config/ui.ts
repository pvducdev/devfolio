export const LAYOUT_CONFIG = {
  editor: {
    defaultSize: undefined,
  },
  panel: {
    defaultSection: "assistant",
    defaultSize: 25,
    maxSize: 40,
    minSize: 15,
  },
  sidebar: {
    defaultSection: null,
    defaultSize: 20,
    maxSize: 40,
    minSize: 15,
  },
  stretchLayout: false,
} as const;
