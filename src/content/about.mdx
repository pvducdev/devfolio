import { CodeGridAccent } from '../components/common/code-grid-accent';
import { PERSONAL_INFO } from '../config/personal';
import {
  nav_social_github,
  nav_social_gitlab,
  nav_social_linkedin,
  page_portfolio_header,
} from '../paraglide/messages.js';

<div className="relative min-h-screen">
  <CodeGridAccent variant="neutral" gridSize="medium" />

  <main className="relative z-10">
    <article className="text-center py-8 space-y-6">
      <header className="inline-block">
        <div className="bg-gradient-to-br from-purple-500/20 via-pink-500/20 to-blue-500/20 px-8 py-3 rounded-lg backdrop-blur-sm">
          <span className="text-sm font-medium tracking-wider">{page_portfolio_header()}</span>
        </div>
      </header>

      <div className="flex justify-center">
        <div
          className="w-32 h-32 rounded-full border-2 border-gray-300 dark:border-gray-700 flex items-center justify-center text-6xl"
          role="img"
          aria-label="Developer profile avatar"
        >
          👨‍💻
        </div>
      </div>

      <section className="space-y-2">
        <h1 className="text-4xl font-bold tracking-tight">{PERSONAL_INFO.name}</h1>
        <div className="border-t-2 border-b-2 border-gray-300 dark:border-gray-700 py-3 max-w-md mx-auto">
          <p className="font-medium">{PERSONAL_INFO.role} · {PERSONAL_INFO.location}</p>
          <p className="text-sm text-gray-600 dark:text-gray-400">
            Age {new Date().getFullYear() - PERSONAL_INFO.dob}
          </p>
        </div>
      </section>

      <nav className="flex justify-center gap-4 text-sm" aria-label="Social media links">
        <a
          href={PERSONAL_INFO.contact.linkedin}
          className="hover:underline"
          target="_blank"
          rel="noopener noreferrer"
        >
          {nav_social_linkedin()}
        </a>
        <span aria-hidden="true">·</span>
        <a
          href={PERSONAL_INFO.contact.github}
          className="hover:underline"
          target="_blank"
          rel="noopener noreferrer"
        >
          {nav_social_github()}
        </a>
        <span aria-hidden="true">·</span>
        <a
          href={PERSONAL_INFO.contact.gitlab}
          className="hover:underline"
          target="_blank"
          rel="noopener noreferrer"
        >
          {nav_social_gitlab()}
        </a>
      </nav>

      <div className="text-sm text-gray-600 dark:text-gray-400">
        <a
          href={`mailto:${PERSONAL_INFO.contact.email}`}
          className="hover:underline"
        >
          {PERSONAL_INFO.contact.email}
        </a>
      </div>

      <blockquote className="border-t border-b border-gray-300 dark:border-gray-700 py-4 max-w-2xl mx-auto">
        <p className="italic text-base">{PERSONAL_INFO.about.shortBio}</p>
      </blockquote>
    </article>
  </main>
</div>
